#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int main()
{
    char cumle[4][1000];
    int i;
    printf("Siiri giriniz:\n");
    for(i=0; i<4; i++){
        fgets(cumle[i],1000,stdin);
    }
    printf("---------------------\n");
    printf("Akrostis: ");
    for(i=0; i<4; i++){
        printf("%c", cumle[i][0]);
    }
    return 0;
}
