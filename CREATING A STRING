#include <stdio.h>
#include <stdlib.h>

int main()
{
    int numbers[5],i;
    printf("Numbers: ");
    for(i=0; i<=4; i++){
        scanf("%d", &numbers[i]);
    }
    printf("Numbers written backwards: \n");
    for(i=4; 0<=i; i--){
        printf("%d, ", numbers[i]);
    }
    return 0;
}
